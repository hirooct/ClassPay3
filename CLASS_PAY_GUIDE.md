# ClassPay 仕様・導入・運用ガイド

## 概要

ClassPay は Google Apps Script と Google スプレッドシートで動作する学級内通貨アプリです。児童口座、会社口座、政府口座、決済、給与、税金、利息、株取引、会社設立・所属管理を一体で扱います。

## 導入手順

1. このリポジトリの全ファイルを Apps Script プロジェクトへ反映します。`clasp` を使う場合はリポジトリ直下で `clasp push` を実行します。
2. Apps Script をウェブアプリとしてデプロイします。
3. デプロイURLの `?p=admin` を開き、初回設定を行います。
4. 管理画面の「不足シートを作成・確認」を押します。既存データは削除せず、足りないシートと列だけを追加します。
5. `BASE_URL` にウェブアプリの `/exec` URLを設定します。
6. 児童、会社、PIN、会社PASSを管理画面から登録します。

更新後は Apps Script の「デプロイを管理」から新しいバージョンを作成してください。コードを反映しただけでは公開中のウェブアプリが旧バージョンのままになる場合があります。

## 主な画面

- `?p=home`: ホーム
- `?p=pay`: 児童から会社・政府への支払い
- `?p=balance`: 残高、取引履歴、所属・退社、株取引、会社設立申請
- `?p=shop`: 会社照会、支払いURL、給与送金
- `?p=admin`: 初期設定、児童・会社管理、会計、履歴、ランキング

## 使用するシート

| シート | 内容 |
|---|---|
| `Users` | 児童ID、氏名、残高、PIN、有効状態 |
| `Shops` | 会社ID、会社名、残高、PASS、株設定、有効状態 |
| `Tx` | 児童・会社に関係する取引履歴 |
| `Config` | 管理PASS、BASE_URL、各種設定 |
| `Holdings` | 児童ごとの保有株数 |
| `CompanyMembers` | 会社所属と入退社履歴 |
| `CompanyApplications` | 会社設立申請と承認結果 |
| `RetirementApplications` | 退職届、社長の審査結果、政府受領状態 |
| `Government` | 政府口座残高 |
| `GovernmentLedger` | 税金、利息、権利料など政府資金の履歴 |
| `CompanySnapshots` | 会社ランキング・成長率用の記録 |

### CompanyMembers の履歴列

`shopId, userId, role, isActive, joinedAt, leftAt, leaveReason, leftBy`

退社や会社削除では行を消しません。`isActive` を `FALSE` にし、退社日時・理由・処理者を記録します。会社の削除も同様に停止扱いとなり、取引履歴と株式保有は保持されます。

## オンライン退職届

1. 児童が残高画面の所属会社から「退職届を出す」を選び、退職理由を入力します。
2. 退職届は会社へ提出され、同時に `RetirementApplications` へ保存されるため政府（admin）でも受領を確認できます。
3. 社長は会社照会ページを開き、会社PASSで退職届を確認します。
4. 社長が承認すると所属が終了し、`CompanyMembers` に退社日時・理由・処理者を記録します。
5. 却下した場合は所属を維持し、却下理由を退職届に記録します。
6. 児童は残高画面、政府はadminの「退職届」タブで結果を確認できます。

社長本人は退職届を提出できません。社長変更は政府（先生）が管理画面で行います。管理画面からの直接退社は、訂正や緊急対応用として残しています。

## 政府口座との連動

- 税金: 児童口座から回収し、政府口座へ加算
- 児童・会社への配付: 政府口座から支出
- 児童・会社利息: 政府口座から支出
- 政府への支払い: 支払先で「政府（権利料など）」を選択し、政府口座へ加算

政府関連の動きは `GovernmentLedger` に記録されます。児童による政府への支払いは `Tx` にも `GOV_PAY` として記録されます。

## 定期利息

毎週月曜日の自動利息は Apps Script の時間主導型トリガーで設定します。対象関数はプロジェクトで使用している週次利息実行関数を指定し、実行ユーザーにスプレッドシート更新権限があることを確認してください。利息原資は政府口座から差し引かれます。

## 運用上の注意

- 会社を完全削除せず停止にするのは、会計・株・所属の追跡可能性を守るためです。
- 社長は児童画面から自己退社できません。管理画面で別の社長へ変更した後に退職届を提出するか、会社を停止します。
- PIN・会社PASSは管理画面でも通常は伏せ字で、表示ボタンを押したときだけ見えます。
- 管理画面の会計履歴はタブを開いたときに遅延読み込みし、初期表示を軽くしています。
- 公開URLを他の先生へ配る前に、各先生自身のスプレッドシートと Apps Script プロジェクトで初期設定してください。

## 更新時の確認

1. 管理画面で「不足シートを作成・確認」を実行
2. 新しいデプロイバージョンを公開
3. 児童のログイン、会社・政府への少額決済、退社、管理会計履歴を確認
4. 問題がなければ本番運用を開始
